/** ANOTAÇÕES DO CURSO  **/

-- CONCATENAR
posso usar , ou + 

-- LET


-- UNDEFINED
 foi declarado mais não tem um valor

 ex: let nome; // declarou a variavel 
 console.log(nome); 

 -- O QUE É ESCOPO?
 Em poucas palavras, escopo é a propriedade que determina onde uma variável pode ser utilizada 
 dentro de um programa, por exemplo, se você declara uma variável dentro de uma função, só aquela 
 função consegue utilizar a variável, e SE você declara uma variável fora de uma função, ela pode 
 ser acessada de qualquer parte daquele mesmo script, pois ela foi declarada globalmente. No 
 Javascript existem 3 tipos de escopos, o global, local e de bloco (adicionado no ES6). Vamos ver 
 em detalhes como cada um deles funciona para var e let a seguir.

 -- ESCOPO LOCAL
 Uma variável é local quando ela é declarada dentro de alguma função, pois isso significa que apenas 
 aquela função consegue enxergá-la. As demais funções do script não conseguem utilizá-la, pois a mesma 
 foi declarada dentro de uma função específica. Neste ponto de vista, podemos dizer que o escopo local 
 é completamente diferente do escopo global. Abaixo vamos ver um exemplo de uma variável declarada 
 localmente com var.

 fonte:https://blog.schoolofnet.com/diferenca-entre-var-e-let-no-javascript/

 -- toString
 converte de numero para String

-- Método
função dentro de um objeto

-- ARRAY X OBJETOS
array [] já objeto{}

-- VALOR X PARAMETRO
arugumento é o valor que é passado para o parametro




 