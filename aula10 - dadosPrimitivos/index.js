/** DADOS PRIMITIVOS */

const nome = 'Joyce';
const nome1 = "Joyce";
const nome2 = `Joyce`; // neste caso consigo usar ${} template string usando ``

const num1 = 10; //number
const num2 = 10.52; // number

let nomeAluno; // undefined = não aponta pra local nenhum na memória (foi declarado mais não tem um valor)

let sobreNomeAluno = null; // Nulo -> não aponta pra local nenhum da memória


/** CONTADOR */

let contador = 1;
contador++; // 2
contador++; // 3
contador++ // 4
console.log(contador);

// INCREMENTO - Primeiro ele incrementa e depois mostra o valor







